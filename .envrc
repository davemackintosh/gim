export VCPKG_ROOT="$(pwd)/vcpkg/"

if ! [[ -d ./vcpkg ]]; then
	git submodule update --init --remote --recursive
	./vcpkg/bootstrap-vcpkg.sh
fi

export VCPKG_DISABLE_METRICS=1

source ~/Downloads/1.3.243.0/setup-env.sh

make compile_commands.json

# vim: ft=bash
